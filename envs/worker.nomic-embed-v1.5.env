# NATS Configuration
NATS_URL=nats://127.0.0.1:5700
STREAM_NAME=EMBED_NOMIC_V1_5
SUBJECT=embedding.request.nomic-embed-v1.5
QUEUE_DURABLE=nomic-embed-v15-wq
QUEUE_GROUP=workers
RESPONSE_PREFIX=embedding.reply
QUEUE_MAX_MSGS=2000
QUEUE_MAX_AGE=30s
ACK_WAIT=30s
MAX_DELIVER=5
MAX_ACK_PENDING=64

# Worker Configuration
WORKER_CONCURRENCY=2

# HTTP Configuration
HTTP_ADDR=:5778

# Model Configuration
MODEL_NAME=nomic-embed-v1.5
MODEL_URL=https://huggingface.co/nomic-ai/nomic-embed-text-v1.5-GGUF/resolve/main/nomic-embed-text-v1.5.f16.gguf
MODEL_PATH=data/models/nomic-embed-v1.5/model.gguf
MODEL_FORMAT=embedding
MODEL_THREADS=8
CTX_SIZE=8192

# Data Directory Configuration  
DATA_DIR=data

# Database Configuration
DB_PATH=data/logs/nomic-embed-v1.5.sqlite

# Monitoring Configuration
MONITORING_TOPIC=monitoring.embedding
BACKPRESSURE_THRESHOLD=5